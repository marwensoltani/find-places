import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.tekup.find.places',
  appName: 'find-places-p',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
